import { ADD_POST_REQUEST } from "../types/postTypes";

const init_state = [];
const postsReducer = (state = init_state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default postsReducer;
